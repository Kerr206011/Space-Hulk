Save/Load in/aus speicherfile
Musik und Optionen


Speicherfile:
    Optionen:
        lauststärke 
    Gameeinstellungen:
        anzahl neuer blips
        anzahl + Art der SM
        Map layout
        Siegesbedingungen
    Higscore/ liste(kann scrollen usw)

später neue Steuerung

Fehler:
    überall when selectedmodel != none einfügen
    overwatch jam macht select bug -> selected_tile muss None
    bug beim danach moven
    shoot braucht kein AP wenn waffe kein ranged hatt
    scroll get nicht ins negative

features:
    open/close door in 3 tiles front
    sm_place braucht rotation
    sm_place braucht liste
    bei win werte auf None / 0 setzten
    not enough cp einfügen
    reload button(AssaultCanon)
    perhaps fix flamer flaming die selbe sekton?
    move mehrmals
    gegner Ki
    rolls als Popup
    move - turn als option nicht popup(same mit move-shoot)
    sm turn und shoot
    modells zoom to scale (neue modelle wenn nah genug ran)
    cutscene modular machen

referenz:
rank : 'sergeant'
weapon : 'fist','AssaultCanon','powerSword,'flamer','chainFist','claws','hammer'