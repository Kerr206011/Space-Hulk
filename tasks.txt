Save/Load in/aus speicherfile
Musik und Optionen


Speicherfile:
    Optionen:
        lauststärke 
    Gameeinstellungen:
        anzahl neuer blips
        anzahl + Art der SM
        Map layout
        Siegesbedingungen
    Higscore/ liste(kann scrollen usw)

später neue Steuerung

Fehler:
    überall when selectedmodel != none einfügen
    overwatch
    gamestate turn nicht aufrufen wenn model tot ist
    shoot braucht kein AP wenn waffe kein ranged hatt
    blip verliert ap wenn move in SM los
    fullturn geht nicht bei reveal
    overwatch in 12

features:
    selected und clicked Modelle bokommen spezielle Bilder
    main menu button 
    scroll permanenet
    door shoot erweitern
    in P2 act/turn einen anzeigebutton und info für die Menge
    AssaultCanon implementation beenden (shoot/melee)
    sm_place braucht rotation
    sm_place braucht liste
    in P2turn waffen und rang des models anzeigen
    bei win werte auf None / 0 setzten
    bilder für:
        modelle durchsichtig
    waffen beim auswählen anzeigen
    flamer / AssaultCanon ammo anzeigen

referenz:
rank : 'sergeant'
weapon : 'fist','AssaultCanon','powerSword,'flamer','chainFist','claws'